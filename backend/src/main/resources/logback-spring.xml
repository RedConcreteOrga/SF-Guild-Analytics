<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!--
        SECURITY: Logback configuration with password masking.
        Masks common sensitive patterns in log output using a message converter.

        Sensitive data that should NEVER appear in logs:
        - Passwords (in any field name variation)
        - JWT tokens (they are bearer credentials — treat like passwords)
        - Authorization headers
        - Database credentials
    -->

    <!-- Import Spring Boot defaults for console/file appenders -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Safe console pattern: structured, readable, no sensitive markers -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Production file appender with rolling policy -->
    <springProfile name="!dev">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>logs/application.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <!-- Rotate daily, keep 30 days -->
                <fileNamePattern>logs/application.%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>30</maxHistory>
                <totalSizeCap>1GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
    </springProfile>

    <!-- SECURITY: Suppress noisy Spring Security debug logs in production -->
    <springProfile name="!dev">
        <logger name="org.springframework.security" level="WARN"/>
        <logger name="org.hibernate.SQL" level="WARN"/>
        <logger name="org.hibernate.type.descriptor.sql" level="WARN"/>
        <!-- Suppress Hibernate parameter logging (can expose data values) -->
        <logger name="org.hibernate.orm.jdbc.bind" level="WARN"/>
    </springProfile>

    <!-- Development profile: verbose but still safe -->
    <springProfile name="dev">
        <logger name="org.springframework.security" level="DEBUG"/>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="com.sf.guildanalytics" level="DEBUG"/>
    </springProfile>

    <!-- Application logger -->
    <logger name="com.sf.guildanalytics" level="INFO"/>

    <!-- Root logger -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

</configuration>
